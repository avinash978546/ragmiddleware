# RAG-Guard dependencies
# Pinned for Python 3.11 compatibility

# Core dependencies
pymupdf==1.24.5
langchain-text-splitters==0.2.1
sentence-transformers==2.7.0
qdrant-client==1.9.1
python-dotenv==1.0.1
requests==2.32.3

# Explicitly pin numpy to avoid numpy 2.0 compatibility issues with torch/sentence-transformers
numpy==1.26.4

# Presidio PII Scanner dependencies
presidio-analyzer==2.2.353
presidio-anonymizer==2.2.353
spacy==3.7.4

# DeBERTa prompt-injection classifier (M3)
transformers==4.44.2
torch>=2.0.0
